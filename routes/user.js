const User = require('../models/User')

const router = require('express').Router()

// get all user
// get a single user
// update a user
// delete a user
// follow a user
// unfollow a user

module.exports = router